import { TimelineContentDirective } from './timeline-content.directive';

describe('TimelineContentDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineContentDirective();
    expect(directive).toBeTruthy();
  });
});
